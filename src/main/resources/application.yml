spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/BD_Modelagem
    username: postgres
    password: Senha123!
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
